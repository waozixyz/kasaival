/* General settings for the entire page */
body {
  max-width:800px;
  margin: auto;
  padding: 0px;
  color: #ba5c46;
  background-color: #280324;
  font-family: sans-serif;
  -moz-text-size-adjust: none;
  -webkit-text-size-adjust: none;
  -mx-text-size-adjust: none;
}

button, input[type="submit"] {
  border: none;
  cursor: pointer;
  background-color: #ba5c46;
  color: #f9c0c7;
}

button:hover, input[type="submit"]:hover {
  background-color: #da7c66;
  color: #ffd0d7;
}

textarea, input[type="text"], input[type="input"], input[type="password"] {
  color: #ffd0d7;
  background-color: #886384;

  border: none;
}

input[type="text"] {
  padding: .5em;

}

textarea {
  min-height: 20em;
  width: 100%;
  margin: 1em 0;
  padding: 1em;
}

/* The project logo in the upper left-hand corner of each page */
div.logo {
  display: inline;
  text-align: center;
  vertical-align: bottom;
  font-weight: bold;
  font-size: 2.5em;
  color: #b10156;
  white-space: nowrap;
}

/* The page title centered at the top of each page */
div.title {
  display: table-cell;
  font-size: 2em;
  font-weight: bold;
  text-align: left;
  padding: 0 0 0 5px;
  color: #b10156;
  vertical-align: bottom;
  width: 100%;
}

/* The login status message in the top right-hand corner */
div.status {
  display: table-cell;
  text-align: right;
  vertical-align: bottom;
  color: #b10156;
  padding: 5px 5px 0 0;
  font-size: 0.8em;
  font-weight: bold;
  white-space: nowrap;
}

/* The header across the top of the page */
div.header {
  display: table;
  width: 100%;
}

/* The main menu bar that appears at the top of the page beneath
** the header */
div.mainmenu {
  padding: 5px 10px 5px 10px;
  font-size: 0.9em;
  font-weight: bold;inherit
  text-align: center;
  letter-spacing: 1px;
  background-color: #3f0018;
  color: white;
}

/* The submenu bar that *sometimes* appears below the main menu */
div.submenu, div.sectionmenu {
  padding: 3px 10px 3px 0px;
  font-size: 0.9em;
  text-align: center;
  background-color: #3f0018;
  color: white;
}
div.mainmenu a, div.mainmenu a:visited, div.submenu a, div.submenu a:visited,
div.sectionmenu>a.button:link, div.sectionmenu>a.button:visited,
div.submenu label {
  padding: 3px 10px 3px 10px;
  color: white;
  text-decoration: none;
}
div.mainmenu a:hover, div.submenu a:hover, div.sectionmenu>a.button:hover,
div.submenu label:hover {
  color: #a06048;
  background-color: white;e1a498
}

/* All page content from the bottom of the menu or submenu down to
** the footer */
div.content {
  padding: 1ex 5px;
}
div.content a { color: #b12196; text-decoration: none }
div.content a:link { color: #b12196; }
div.content a:visited { color: #b12196; }
div.content a:hover { color: #d940b7;  }

/* Some pages have section dividers */
div.section {
  padding: 3px 3px 0 3px;
  font-size: 1.2em;
  font-weight: bold;
  background-color: #a09048;
  color: white;
  white-space: nowrap;
}

/* The "Date" that occurs on the left hand side of timelines */
div.divider {
  font-size: 1em; font-weight: bold;
  padding: .25em;
  margin: .2em 0 .2em 0;
  float: left;
  clear: left;
  white-space: nowrap;
}

/* The footer at the very bottom of the page */
div.footer {
  font-size: 0.8em;
  margin-top: 12px;
  padding: 5px 10px 5px 10px;
  text-align: right;
  background-color: #3f0018;
  color: white;
}

/* Hyperlink colors */
div.footer a { color: white; }
div.footer a:link { color: white; }
div.footer a:visited { color: white; }
div.footer a:hover { background-color: white; color: #558195; }

/* <verbatim> blocks */
pre.verbatim {
  background-color: #f5f5f5;
  padding: 0.5em;
  white-space: pre-wrap;
}

/* The label/value pairs on (for example) the ci page */
table.label-value th {
  vertical-align: top;
  text-align: right;
  padding: 0.2ex 2ex;
}

div.forumSel, td.tktDspValue {
  color: #fbb4a6; 
  background-color: #380324;
}
pre.verbatim {
  color: #fbf4c6; 
  background-color: inherit;
}

.debug {
  color: inherit;
  background-color: inherit;
  border: none;
}

table.report {
  border: none;
  margin: 1em 0;
  color: #b10156;
}
td {
  padding: .5em;
}

select {
  border: none;
  color: #ffd0d7;
  background-color: #886384;
}
